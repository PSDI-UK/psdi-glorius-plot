apiVersion: apps/v1
kind: Deployment
metadata:
  name: psdi-glorius-plot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: psdi-glorius-plot
  template:
    metadata:
      labels:
        app: psdi-glorius-plot
      annotations:
        elastic.co/dataset: kubernetes.container_logs.psdi_glorius_plot
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - psdi-glorius-plot
                topologyKey: "kubernetes.io/hostname"
      containers:
        - name: psdi-glorius-plot
          image: psdi-glorius-plot
          volumeMounts:
            - mountPath: /tmp
              name: uploads
          ports:
            - containerPort: 8000
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi" # this value may need to increase if application stability declines
              cpu: "1000m"
      imagePullSecrets:
        - name: github-psdi-registry-credentials
      volumes:
        - name: uploads
          emptyDir:
            sizeLimit: 1Gi
