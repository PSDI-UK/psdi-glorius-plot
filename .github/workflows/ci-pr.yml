# Workflow for continuous integration tasks triggered on non-draft pull requests to main and release

name: CI - PRs
run-name: CI - PRs
on:
  workflow_dispatch:
  pull_request:
    types: ["opened", "edited", "reopened", "synchronize", "ready_for_review"]
    branches: ["main", "release"]
    # No need to run tests for doc-only changes, maintenance code, or deployment code not covered by tests
    paths-ignore:
      - "**.md"
      - "Dockerfile"
      - "LICENSE"
      - "**.txt"
      - "deploy/**"
      - "scripts/fetch-common-style.*"

jobs:
  test-python:
    if: github.event.pull_request.draft == false
    uses: PSDI-UK/psdi-github-workflows-public/.github/workflows/test-python.yml@30e89ff83aa466fe35cad7b78444fbcedf345df9
    with:
      source_dir: psdi_glorius_plot

  test-gui:
    if: github.event.pull_request.draft == false
    name: Run full tests of the gui locally
    uses: PSDI-UK/psdi-github-workflows-public/.github/workflows/test-gui.yml@30e89ff83aa466fe35cad7b78444fbcedf345df9
    with:
      simple: true
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
